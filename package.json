{
  "name": "review-comments-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "docs-site": "pnpm --filter docs-site start -p 3005",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
